name: couldai_user_app
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.7.2

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  # Utiles para fechas
  intl: ^0.19.0
  # Para tomar fotos
  image_picker: ^1.0.7
  # Para iconos bonitos
  lucide_icons: ^0.3.0
  # Para compartir/exportar archivos (futuro excel)
  share_plus: ^9.0.0
  path_provider: ^2.1.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
